name: CI - Mon Premier Pipeline  # Nom du workflow

on:
  push:
    branches: [ "main", "develop" ]
    paths:
      - "backend/**"
      - "frontend/**"

  pull_request:
    branches: [ "main" ]

  schedule:
    - cron: "0 2 * * *"   # Tous les jours à 2h du matin

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Afficher un message
        run: echo "Pipeline exécuté avec succès 🚀"
